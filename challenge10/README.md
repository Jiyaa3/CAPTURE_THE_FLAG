# Mystery Project

Just a normal looking project. Or is it?

Hint: the flag isn't sitting in any single file right now — it's
scattered through this project's *history*. Use `git log` and
`git show`/`git diff` to dig through every commit and reassemble it.

## Changelog
- v1.2.0: performance improvements
